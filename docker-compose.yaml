version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: java-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: space_fleet_db